# Local-dev Mosquitto config.
# Phase 5 hardening: replace allow_anonymous with per-car password/cert auth
# and enable TLS on 8883.

listener 1883
protocol mqtt

# WebSocket listener (browser test clients / future direct subscriptions)
listener 9001
protocol websockets

allow_anonymous true

persistence true
persistence_location /mosquitto/data/
