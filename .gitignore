/node_modules
/dist
/coverage
*.log
.env
.env.local
*.tsbuildinfo
.DS_Store
frontend/node_modules
frontend/dist
frontend/.angular

# Local developer scratch — talks to a live broker and database, and creates
# and deletes accounts. Not part of the repo's test suite.
local_smoke_test.sh
local_live_smoke_test.js

# Local agent guidance — never committed.
CLAUDE.md
skills-lock.json
.claude
.agents
.codex
